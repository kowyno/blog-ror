<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Blog RoR" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="w-full bg-white border-b shadow-sm mb-4">
      <div class="max-w-6xl mx-auto flex items-center justify-between px-6 py-3">
        <!-- Logo/Brand -->
        <div class="flex items-center gap-2">
          <%= icon_upload %>
          <%= link_to "Blog", root_path, class: "text-gray-700 font-semibold hover:text-blue-600 transition" %>
        </div>

        <!-- Navegación condicional basada en autenticación -->
        <div class="flex items-center gap-4">
          <% if user_signed_in? %>
            <!-- Usuario autenticado -->
            <div class="flex items-center gap-4">
              <span class="text-gray-700">
                Hola, <%= user_display_name(current_user) %>
              </span>      
              
              <%= link_to profile_path, class: "flex items-center gap-2 text-gray-700 hover:text-blue-600 transition" do %>
                <%= icon_profile %>
                Perfil
              <% end %>

              <%= button_to destroy_user_session_path, method: :delete, class: "flex items-center gap-2 text-red-600 hover:text-red-700 transition", form: { class: "inline" } do %>
                <%= icon_logout %>
                Salir
              <% end %>
            </div>
          <% else %>
            <!-- Usuario no autenticado -->
            <div class="flex items-center gap-3">
              <%= link_to new_user_session_path, class: "flex items-center gap-2 px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition" do %>
                <%= icon_user %>
                Iniciar sesión
              <% end %>
              
              <%= link_to new_user_registration_path, class: "flex items-center gap-2 px-4 py-2 text-white bg-blue-600 rounded-lg hover:bg-blue-700 transition" do %>
                <%= icon_user_add %>
                Registrarse
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </nav>
    <%= render_flash_messages %>
    
    <main class="mx-auto mt-28 px-5">
      <%= yield %>
    </main>
    
  </body>
</html>